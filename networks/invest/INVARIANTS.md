# INVARIANTS - invest

<!-- generated by pcnn; edit neurons/, not this file -->

Every pinned neuron plus every CON/DEC/GOT neuron at salience >= 0.85, rendered in full.
Contradicting any entry below requires saying so explicitly first.

## INVEST.DEC-001 - The scan universe is smallcap and midcap from the Nasdaq screener API, not S&P 500 or Nasdaq-100

- layer: DEC | salience: 0.90 | confidence: verified | since: 2026-09-17
- fact: src/universe.py builds the default scan universe from the Nasdaq screener endpoint https://api.nasdaq.com/api/screener/stocks?tableonly=true&limit=10000&offset=0&exchange=<EX> across NASDAQ, NYSE and AMEX, bucketed by market cap into smallcap and midcap sources, giving roughly 7000 tickers before filtering. The canonical scan is --universe smallcap,midcap,wsb.
- why: Index membership selects for companies that already completed their growth; scanning the S&P 500 returned only high-priced, topped-out names, which is the opposite of the stated goal of finding future-promising stocks.
- rejected: S&P 500 and Nasdaq-100 constituent lists scraped from Wikipedia, which remain available as the sp500 and nasdaq100 sources but are no longer the default.
- anchors: config/weights.yaml, src/universe.py
- connected: constrained_by GOT-002, implements IDENT-001

## INVEST.DEC-002 - Scores are shrunk toward the mean by coverage so thin-data tickers cannot top the ranking

- layer: DEC | salience: 0.90 | confidence: verified | since: 2026-09-17
- fact: src/scoring.py multiplies each ticker's weighted score by a coverage-based shrinkage term and drops any ticker whose coverage falls below a configured floor entirely, in addition to flagging low_confidence below min_coverage_for_confidence (0.60). A market capitalisation ceiling filter was added at the same time.
- why: Without shrinkage, tickers with almost no data (small regional banks such as RZC, AMTB, AMAL, OBK) scored highest because the weights of their missing factors were renormalised away, turning absent data into an advantage and concentrating the top of the ranking in one sector.
- rejected: Relying only on the low_confidence flag and leaving thin-data tickers in the ranking for the reader to discount.
- anchors: config/weights.yaml, src/scoring.py, tests/test_scoring.py
- connected: depends_on GLO-001, refines DEC-003

## INVEST.DEC-003 - Factors whose data coverage is too thin are auto-disabled and the remaining weights renormalised

- layer: DEC | salience: 0.87 | confidence: verified | since: 2026-09-17
- fact: src/scoring.py detects factors available for too few tickers in a run, removes them from the calculation, renormalises the surviving weights so they still sum to the configured total, and reports the removals in diagnostics under auto_disabled, which the dashboard displays.
- why: The user required that an over-restrictive parameter such as presence on the Reddit wallstreetbets chart be sacrificeable rather than silently eliminating most candidates.
- rejected: Hard-filtering on restrictive parameters, and scoring missing factors as zero, both of which penalise tickers for data absence rather than for weakness.
- anchors: src/scoring.py, tests/test_scoring.py
- connected: depends_on DAT-001

## INVEST.CON-001 - The in-dashboard assistant must stay free and offline, answering only from data embedded in the page

- layer: CON | salience: 0.96 | confidence: verified | since: 2026-09-17
- fact: src/assistant_ui.py emits a chat panel into section IV of output/dashboard.html that resolves questions about tickers and parameters with a client-side intent engine over the computed data already serialised into the page. No paid language-model service may be called, and no network request is made at answer time.
- why: The user requested the chat feature with the explicit condition that it be free, so any design that calls a hosted model API violates the requirement regardless of quality gain.
- anchors: src/assistant_ui.py, src/report.py
- connected: constrained_by IDENT-001

## INVEST.CON-002 - All user-facing text in invest is Turkish, including parameter names, docs and dashboard labels

- layer: CON | salience: 0.95 | confidence: verified | since: 2026-09-17
- fact: Every parameter in config/weights.yaml carries a name_tr field, docs/PARAMETRELER.md explains all parameters in Turkish, and README.md plus the dashboard and watchlist HTML render their labels, risk levels and explanations in Turkish. Output files are written and read with explicit UTF-8 encoding, and generated HTML must carry a charset meta tag.
- why: The user works in Turkish and asked for a detailed Turkish explanation file covering every parameter, so English-only additions would be unreadable to the sole intended reader.
- anchors: config/weights.yaml, docs/PARAMETRELER.md, src/report.py
- connected: constrained_by DAT-001

## INVEST.GOT-001 - yfinance fails TLS on this machine until the certifi bundle is copied to an ASCII-only path

- layer: GOT | salience: 0.88 | confidence: verified | since: 2026-09-17
- fact: Fetching data through yfinance on this Windows machine requires copying certifi.where() to C:\claude-certs\cacert.pem and exporting CURL_CA_BUNDLE and SSL_CERT_FILE to that path before running any command that touches Yahoo Finance.
- why: The default certifi bundle sits under a user profile directory containing the non-ASCII character in MSİ, which the underlying TLS stack cannot open, so certificate verification fails before any request is made.
- anchors: README.md, src/providers/yahoo.py
